<div class="body">
  <div class="container">
    <h1>Editing Book</h1>
    <%= form_with model:@book, local:true do |f| %>
      <% if @book.errors.any? %>
         <h3 class="error_explanation"><%= @book.errors.count %> error prohibited this book from being saved:</h3>
         <ul class="error_explanation">
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
         </ul>
    　<% end %>
      <h4>Title</h4>
      <%= f.text_field :title ,class:"col-xs-12 book_title"%>
      <br><br>
      <h4>Body</h4>
      <%= f.text_area :body,class:"col-xs-12 book_title" %>
      <%= f.submit 'Update Book',class:"btn btn-primary col-xs-12 margin" %>

    <% end %>

    <span>
        <%= link_to "Show", book_path(@book.id) %> |
    </span>
    <span>
        <%= link_to "Back", books_path%>
    </span>
  </div>
</div>

